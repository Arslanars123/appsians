name: appsians
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  pinput: ^5.0.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  syncfusion_flutter_charts: ^26.1.40
  pie_chart: ^5.4.0
  fluttertoast: ^8.2.6
  shared_preferences: ^2.2.3
  connectivity_plus: ^6.0.3
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo/
    - assets/images/logo/tigerLogo.png
